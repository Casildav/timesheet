{
  "name": "timesheet-app",
  "version": "1.0.0",
  "description": "Timesheet PWA for tracking hours and expenses",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}
